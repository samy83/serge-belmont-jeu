# npm 10.9 plante ("Cannot read properties of null (reading 'edgesOut')") sur la
# resolution des peer-dependencies de vitest 4 ; ce reglage contourne le bug
# et n'a aucun effet sur les versions installees (voir package-lock.json).
legacy-peer-deps=true
audit=false
fund=false
